# Sao chép nội dung này vào mục "Secrets" của app trên share.streamlit.io
# (Settings -> Secrets). KHÔNG commit file secrets.toml thật lên GitHub.
#
# Nếu điền sẵn ở đây, app sẽ tự động điền vào ô API Key khi mở lên — bạn
# vẫn có thể ghi đè bằng cách nhập key khác ở sidebar.

PINECONE_API_KEY = "dán-pinecone-api-key-của-bạn"
GROQ_API_KEY = "dán-groq-api-key-của-bạn"
